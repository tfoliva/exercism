#include "hamming.h"

int compute(const char* lhs, const char* rhs) {
        int diff_count = 0;
        // traverse string
        // compare each char in string
        // if length of lhs and rhs is different
        return diff_count;
}
